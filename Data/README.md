# Data Directory

The analysis expects local source and intermediate data under this directory, but raw and record-level data are not tracked in Git.

See `DATA_AVAILABILITY.md` for source descriptions and redistribution limits. The pipeline writes regenerated outputs under `Data/analysis/` and `Data/intermediate/`, both of which are ignored by Git.
